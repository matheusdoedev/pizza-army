import { Header, PizzaAnimation, ProductSelector } from "@/components";

const Product = () => {
  return (
    <div>
      <Header />
      <ProductSelector />
      <PizzaAnimation />
    </div>
  );
};

export default Product;
